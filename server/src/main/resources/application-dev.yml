spring:
  datasource:
    url: jdbc:mysql://10.224.5.50:3306/ritual_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: ritual_db_manger
    password: ritual_db_manger
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: 123456
      database: 1
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql
      data-locations: classpath:db/data.sql

logging:
  level:
    com.ritual: debug
